language: node_js
node_js:
    - "node"
script:
    - yarn build
    - yarn export
branches:
    only:
        - source

deploy:
    provider: pages
    skip_cleanup: true
    github_token: $github_token
    local_dir: out
    target_branch: master
    on:
        branch: source
